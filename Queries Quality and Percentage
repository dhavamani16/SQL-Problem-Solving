select query_name,
ROUND(SUM(rating/position)/ COUNT(query_name),2) AS quality,
ROUND(
    SUM(CASE WHEN rating<3 THEN 1 ELSE 0 END)*100/COUNT(query_name),2) as poor_query_percentage
from Queries
group by (query_name)
